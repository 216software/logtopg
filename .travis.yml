---

language: python
python:

  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

# command to install dependencies
install:
  - "pip install ."
  - "pip install -r requirements.txt"

# command to run tests
script: nosetests

addons:
    postgresql: "9.3"

before_script:
  - psql -c "create user logtopg with password 'l0gt0pg';" -U postgres
  - psql -c "create database logtopg with owner logtopg;" -U postgres
  - psql -d logtopg -c "create extension ltree;" -U postgres
